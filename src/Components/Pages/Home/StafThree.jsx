import React from 'react';
import useStafs from '../../../Hooks/useStafs';

const StafThree = () => {
    const [stafs] = useStafs();


    return (
        <div className='w-full'>
            
        </div>
    );
};

export default StafThree;